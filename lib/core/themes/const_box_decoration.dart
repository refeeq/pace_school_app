import 'package:flutter/material.dart';

class ConstBoxDecoration {
  static BoxDecoration whiteDecoration = const BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.only(
        // topLeft: Radius.circular(25),
        // topRight: Radius.circular(25),
        ),
  );
}
